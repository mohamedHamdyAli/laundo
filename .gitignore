*.log
.DS_Store
.env
.playwright-mcp
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
/test-results
/storage/playwright-report
/playwright/.cache
repair.tmp.php

# The Second Brain's parse cache. `data/` is committed — it is the brain, and a
# teammate should get it without rebuilding — but `cache/` is per-machine and
# rebuilt in about two seconds.
/.second-brain/cache
